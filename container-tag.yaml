# The Jenkins job requires a tag to build the Docker image.
# Global-JJB script assumes this file is in the repo root.
#
# Preview Release: So give it a 0.1.0 tag.
# Updating the preview release for 0.2.0
---
tag: 0.2.0
